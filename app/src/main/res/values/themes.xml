<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NiceStart2" parent="Theme.Material3.Light.NoActionBar">
        <!-- Personaliza tu tema claro aquí. -->
        <item name="android:windowIsTranslucent">true</item> <!-- Para que desaparezca el inicio de sesión con la app y entre directamente al splash -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.NiceStart2" parent="Base.Theme.NiceStart2" />

    <style name="Base.Theme.NiceStart2.ActionBar" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Cambia el color de fondo de la Toolbar -->
        <item name="colorPrimary">@color/blue</item>
        <!-- Cambia el color del texto de la Toolbar -->
        <item name="colorOnPrimary">@color/black</item>
    </style>
</resources>
